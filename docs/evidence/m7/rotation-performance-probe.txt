M7 primary rotation and performance probe, follow-up 2026-08-01

Scope:
- developer-only Vulkan probe on the physical Thor primary presentation
- controllable castle-grounds checkpoint loaded from an app-private save state
- Thor-default rotation fix: commit
  `54410dc0934886fb6f4ddaba4f59ce3772a9eb1f`, pushed as `main` to
  `https://github.com/joeblack2k/ThorDS`
- no ROM, save state, screenshot, log dump, device identifier or private
  capture retained in this repository

Rotation:
- the debug launch extra
  `io.github.joeblack2k.thords.extra.VULKAN_ROTATE_180=true` remains available
  for non-Thor reproductions
- the product config now enables the same primary-only rotation automatically
  when `Build.MANUFACTURER=AYN` and `Build.MODEL=AYN Thor`
- classifier: `M7Probe: dual-uv direct=0 rect=1920x1080 safeX=240 safeWidth=1440`
- native transform: `M7Probe: rotate180 surface=1 vertices=12`
- the world transform rotates output quad positions; the packed UI overlay uses
  the native top-down buffer mapping in its dedicated draw mode
- the lower external presentation kept its ordinary configuration because the
  product rotation flag is mapped only into the primary Activity presentation
  config
- a fresh GitHub Prod debug launch with the widescreen probe but without the
  rotation extra still logged `rotate180 surface=1`, proving the Thor default
- a final physical-surface screenshot from the Castle Garden checkpoint
  showed the world upright and the pause overlay text readable; this closes
  the primary framebuffer orientation smoke

Packed UI correction:
- the first rotation implementation applied the primary vertex transform to
  both the high-resolution world and the packed plane-1 overlay
- the packed overlay uses the native top-down packed-buffer orientation, so
  its primary probe draw now keeps the safe-area geometry and samples
  `sourceY = fragUv.y * 192` in draw mode 8
- the follow-up physical pause screen rendered readable `CASTLE SECRET STARS`
  and `TOUCH TO SELECT`; the lower external presentation stayed unchanged

Performance observation:
- the first instrumented castle-grounds debug run reported approximately
  38-40 FPS
- `VulkanPerf[Instance]` reported approximately 25.6 ms average CPU time
- `VulkanPerf[Pacing]` reported `presented=120`, `fallback=240`, and no
  acquire, record or submit failures in the sampled interval
- the instrumented run retained renderer debug tools, so it remains diagnostic
  evidence only
- the follow-up GitHub Prod debug APK was rebuilt with target-local `-O2` for
  the debug `core` and Android frontend targets; the compile database confirms
  the flag and debug symbols remain enabled
- with renderer debug tools disabled, the same castle-grounds save state
  reported `FPS: 60` in two captures separated by eight seconds
- the optimized debug result is a strong scene-level performance improvement;
  a follow-up 10-second Slot-2 movement smoke held `FPS: 60` and produced no
  ThorDS FATAL or ANR
- this is not yet a full realtime acceptance result across transitions and
  the remaining M7 geometry checks

Result: PARTIAL_PASS
