M7 W-06 Castle Garden bottom-primary raster probe
Date: 2026-08-01
Scope: developer Vulkan capture, Castle Garden checkpoint

Result: PASS

Probe:
- The existing app-private Castle Garden checkpoint was already restored
  through the direct debug route; no title or intro replay was used.
- A one-frame live Vulkan capture requested only `packedbottom` and returned
  `success=1`.
- The capture log reported:
  `captureKindsFirst=packed_bottom_primary`,
  `packedBottomPrimary=256x192:49152`,
  `pixels=49152`, `opaque=49152`, `nonBlack=48635`,
  `alphaRange=255-255` and `magenta=0`.
- The resulting native bottom raster has the expected `256x192` 4:3
  dimensions and contains non-empty Castle Garden bottom-screen content.
- The filtered device log contained no `FATAL EXCEPTION`, `ANR in` or
  `am_anr` entry during the probe.

Final physical-surface measurement:
- A debug-only in-app PixelCopy captured the lower Presentation only after an
  exact native frame step and two successful presenter records.
- The paired source and final capture were tied to emulator frame 272.
- Source geometry was `256x192`; final physical geometry was `1240x1080`.
- Presenter metadata placed the lower raster at `x=0,y=75,width=1240,height=930`
  with primary rotation disabled. The destination and source are both exactly
  4:3, for `0.0%` aspect error.
- After explicitly accounting for the debug source's red/blue byte order, the
  normalized physical rect matched the source pixel-for-pixel.
- The known lower-HUD bomb-body and digit-4 masks each measured `0.0%` aspect
  delta from source to physical output.

Touch-grid rerun:
- The full 3x3 physical grid mapped to DS
  `0/128/255 x 0/96/191`.
- The upper and lower black-bar probes emitted no mapped DS touch.
- Android intermittently dropped the absolute injected display point `(0,75)`;
  `(1,75)` and `(0,76)` both mapped to DS `(0,0)`. This is an input-injection
  edge, not a layout-mapping discrepancy.

Decision:
- W-06 is `PASS`: final lower rect, orientation, aspect and exact touch mapping
  are measured on the current Thor APK.
- The top UI-safe W-04/W-05 path was subsequently measured and passes
  independently in `castle-garden-w04-w05-ui-geometry.txt`.

Safety:
- No ROM bytes, save-state bytes, private screenshots, device identifiers or
  credentials are retained in this evidence file.
- Bob-omb Battlefield is not part of this gate; Castle Garden is the sole M7
  representative scene.

Machine-readable evidence:
- `docs/evidence/m7/surface-geometry-analysis.json`
- `docs/evidence/m7/w06-touch-grid.json`
