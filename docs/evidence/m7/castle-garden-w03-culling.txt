M7 W-03 Castle Garden side-region culling proof
Date: 2026-08-01
Scope: exact-step 1920x1080 developer-widescreen final-primary sequence

Result: PASS

Method:
- The Castle Garden checkpoint was restored directly; no title or intro replay
  was used.
- A 16-frame in-app sequence advanced one exact emulator frame per sample.
- Frame IDs covered `2482..2498`; each sample had two complete presenter
  records, full-width top draw mode `384`, primary rotation enabled and an
  opaque final surface.
- The named animated landmark was a Castle Garden waterfall mist puff.
- Its right-side ROI was `x=1680,y=200,width=240,height=160`, wholly outside
  the centered native 4:3 boundary at `x=1680`.

Measurements:
- The landmark remained wholly inside the right-side ROI for ten consecutive
  exact samples, sequence indices `5..14`.
- Five distinct center positions were observed.
- Horizontal center displacement was `-52.5` pixels.
- Every retained landmark mask was opaque and non-empty.

Decision:
- W-03 is `PASS`. A named animated world landmark remained visible and moved
  across exact consecutive frames in newly exposed 16:9 side content.

Safety:
- No ROM bytes, saves, savestates, private screenshots, device identifiers,
  credentials or tokens are included.
- Bob-omb Battlefield was not used.

Machine-readable evidence:
- `docs/evidence/m7/widescreen-proof-analysis.json`
