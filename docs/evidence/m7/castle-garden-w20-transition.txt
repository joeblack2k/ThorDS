M7 W-20 Castle Garden transition proof
Date: 2026-08-01
Scope: renderer-internal world-pause-world and key-door cinematic/scene transition

Result: PASS

World-pause-world:
- A bounded in-app presenter trace captured 192/192 records without host
  screenshot polling.
- There were zero sequence gaps, frame regressions or consecutive duplicate
  presenter frame IDs.
- Top produced 96 `1920x1080` records with structured world/UI draw modes and
  top-only rotation. Bottom produced 96 `1240x1080` records without rotation.
- The pause interval was bracketed by world presentation on both sides.

Castle Garden key-door transition:
- The owner explicitly selected Castle Garden as the representative
  painting/star-style transition equivalent; Bob-omb Battlefield was not used.
- A debug-only fixture set the exact European SM64DS initial-castle-key flag
  while emulation was paused. It neither reads nor writes the ROM or save file.
- The app-private interior checkpoint started with Yoshi facing the locked
  castle door. Twelve neutral warmup frames discarded post-load renderer
  initialization, then `leftY=-1` drove one continuous exact-step capture.
- The 384 recorded frames covered renderer IDs `12..396`, from closed door
  through key presentation, key insertion, unlock animation, Yoshi entering
  and the two door halves opening.
- Every frame advanced exactly once and had two complete presenter records.
- Final-primary and internal 3D sequences each contained 192 distinct hashes,
  consistent with the title's 30fps update cadence.
- Maximum repeated-hash run was three presentations; no hash reappeared after
  a different run.
- All 384 final-primary frames were fully opaque and non-black. There were no
  rect failures.
- One internal `frameReady` diagnostic was false during the transition, but
  that frame advanced exactly, had both presenter records and had valid opaque
  final/source pixels. It is retained in the report and is not a missing frame.
- Closed-to-open endpoint change was `62.6346%` of final-primary pixels and
  `61.4807%` of internal 3D-source pixels.

Decision:
- W-20 is `PASS`. Both the world-pause-world and owner-selected Castle Garden
  cinematic/scene transition have continuous internal evidence with no
  black, transparent, missing or stale-return flash.

Safety:
- Public evidence contains aggregate text/JSON only.
- No ROM bytes, saves, savestates, raw logs, private screenshots, device
  identifiers, credentials or tokens are included.

Machine-readable evidence:
- `docs/evidence/m7/presenter-trace-analysis.json`
- `docs/evidence/m7/transition-proof-analysis.json`
