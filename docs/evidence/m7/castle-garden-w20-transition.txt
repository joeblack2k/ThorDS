M7 W-20 Castle Garden world-to-pause-to-world transition probe
Date: 2026-08-01
Scope: physical Castle Garden transition and live capture stability only

Result: PARTIAL_WITH_OBSERVED_TRANSITION

Capture:
- 180/180 live captures completed.
- Frame ids were unique and contiguous: 1077..1256.
- 180/180 metadata records were complete.
- 180 valid 256x384 RGBA screen PNGs and one manifest were produced.
- The checked classifier fields each had one distinct value with zero changes.
- screenSwapLatched was true for 180/180 samples.
- frontBufferLatched was 1 for 90 samples and 0 for 90 samples.

Transition attempt:
- Two KEYCODE_BUTTON_MODE pause attempts were sent.
- Observed pause transitions: 0.
- The inspected checkpoint samples remained Castle Garden world frames;
  no pause overlay was observed.
- 90/179 adjacent PNG pairs were byte-identical. The repeated-frame count
  prevents a clean no-previous-frame transition pass.
- No visible flash was found in the four inspected samples, but that is not
  sufficient to pass W-20 without an observed pause transition.

Stability:
- The successful capture window had 0 filtered FATAL EXCEPTION or ANR matches.
- One earlier no-ready setup attempt produced an app ANR before the successful
  capture. It is recorded as a failed setup attempt, not as a green W-20 run.
- Device and local temporary capture artifacts were deleted after aggregation.
- No ROM bytes, save files, private screenshots or device identifiers are
  retained in this evidence.

Direct final-primary transition:
- The Castle Garden checkpoint was loaded with the existing direct debug route
  and resumed without replaying the title or intro.
- A `TAP_INPUT` START command with `duration_ms=100` returned `success=1`.
  After approximately 200 ms, the final primary showed the readable
  `CASTLE SECRET STARS` pause overlay and `TOUCH TO SELECT`.
- A second START command with `duration_ms=100` also returned `success=1`.
  After approximately 200 ms, the final primary returned to the Castle Garden
  world view.
- All three final-primary screenshots were `1920x1080` and fully opaque.
  The world screenshots each had `2,073,600` non-black pixels; the pause
  overlay screenshot had `1,974,510` non-black pixels.
- No filtered `FATAL EXCEPTION`, `ANR in` or `am_anr` entry occurred during
  this direct transition attempt.

Short transition soak:
- Three pre-pause final-primary samples were captured at approximately
  50 ms intervals; all were opaque world frames with `2,073,600` non-black
  pixels.
- Ten pause samples at the same host polling interval were all opaque pause
  frames with `1,974,510..1,974,780` non-black pixels; no black or transparent
  intermediate was observed.
- After the second START command, the first sample still showed the pause
  overlay and the following nine samples showed the Castle Garden world.
  Every sample was opaque and non-black; the returned world frames had
  `2,073,600` non-black pixels.
- This is a bounded host-polled soak, not a continuous final-surface capture,
  so it strengthens but does not replace longer no-flash cadence evidence.

Decision:
- W-20 advances to `PARTIAL_WITH_OBSERVED_TRANSITION`: the direct final-primary
  sequence now proves a real Castle Garden world-to-pause-to-world transition.
- W-20 remains open for longer no-flash/soak coverage and exact cadence
  evidence between the world, pause and restored-world states.
- Object aspect, side-region culling and HUD/glyph geometry remain open.
