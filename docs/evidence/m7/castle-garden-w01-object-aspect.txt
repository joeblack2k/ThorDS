M7 W-01 Castle Garden object-aspect proof
Date: 2026-08-01
Scope: paired native/probe exact-step final-primary sequences

Result: PASS

Method:
- Castle Garden is the owner-selected M7 representative scene.
- The same app-private checkpoint was loaded for native 4:3 and developer
  widescreen launches. Private states and captures were not retained here.
- Each launch produced 16 exact consecutive final-primary frames after a
  controlled right-camera input of `x=-0.8`.
- Every frame was opaque. Presenter metadata placed native top output at
  `x=240,y=0,width=1440,height=1080` and probe output at
  `x=0,y=0,width=1920,height=1080`.
- The named reference object was the stable Yoshi green-head silhouette in
  ROI `x=800,y=520,width=320,height=172`.

Measurements:
- Paired frame 9: native aspect `1.288136`, probe aspect `1.279661`,
  delta `0.6579%`.
- Paired frame 15: native aspect `1.225806`, probe aspect `1.209677`,
  delta `1.3158%`.
- Both controlled-camera samples are within the W-01 `2.0%` tolerance.

Decision:
- W-01 is `PASS`. The same named object retained its aspect through the
  game-side projection and final structured presenter paths across controlled
  camera movement.

Safety:
- No ROM bytes, saves, savestates, private screenshots, device identifiers,
  credentials or tokens are included.
- Bob-omb Battlefield was not used.

Machine-readable evidence:
- `docs/evidence/m7/widescreen-proof-analysis.json`
