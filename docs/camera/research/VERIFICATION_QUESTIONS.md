# Questions Luna must answer with evidence

1. What is the exact current submodule SHA?
2. What is the live register holding the camera pointer at the candidate hook?
3. What exact instruction range implements digital yaw in the EU ROM?
4. Can that range contain the replacement and legacy fallback?
5. What camera update frequency is measured?
6. Which path draws the user's visible arrows?
7. Is `0x020FC0C0` patched after overlay load?
8. What exact condition triggers the observed sound?
9. Which states call `func_0200bb28`?
10. Which flags or arguments reliably identify scripted/non-normal states?
11. Does clearing `0x60` during analog input preserve collision?
12. Does the original `ApproachLinear` fully follow the maximum target delta?
13. Can a state load retain stale camera protocol state?
14. Are right-stick axes duplicated by stored user mappings?
15. Does Original remain byte/runtime-equivalent after relaunch?
