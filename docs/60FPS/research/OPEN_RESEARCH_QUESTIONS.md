# Open research questions

1. Does Bob-omb Battlefield reliably expose cadence 2 in current direct boot?
2. Which active-object vtable slot actually invokes Stage Behavior?
3. Are Stage/entry function addresses block starts in the active JIT?
4. Which initializers overwrite cadence after launch/state load?
5. Does AR execute before or after the ARM9 cadence comparison on each VBlank?
6. Which fixed-step systems cause the community patch's logic errors?
7. Is 125% ARM9 enough on Thor, or is 150% the minimum?
8. Does True Widescreen increase enough GPU/CPU load to change the OC ratio?
9. Does RA Casual processing materially affect stress-scene headroom?
10. Which timing fields need save-state compatibility metadata?
